# Schedule Agent Dependencies
# Core
pyyaml>=6.0
pydantic>=2.0
python-dateutil>=2.8
python-dotenv>=1.0

# Content memory (PDF/DOCX/etc. → Markdown)
markitdown>=0.1.0

# LLM
openai>=1.0
httpx>=0.24
mcp>=1.0

# Markdown parsing & HTML to text (Feishu filter)
markdown-it-py>=3.0.0
beautifulsoup4>=4.12.0

# Web / API server (Feishu integration, future web UI)
fastapi>=0.115.0
uvicorn>=0.30.0

# Feishu / Lark SDK（长连接事件订阅）
lark-oapi>=1.0.0

# Optional browser automation for SJTU JW login
playwright>=1.49.0

# CLI
prompt_toolkit>=3.0
rich>=14.0

# Testing
pytest>=7.0
pytest-asyncio>=0.21

# Development
black>=23.0
isort>=5.12
